require 'spec_helper'

describe TerminalThesaurus do
  it 'has a version number' do
    expect(TerminalThesaurus::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
